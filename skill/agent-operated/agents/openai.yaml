interface:
  display_name: "Agent Operated"
  short_description: "Route operations with actor and candidate binding"
  default_prompt: "Use $agent-operated to route this GitHub task through the required actor, Operation, and candidate-binding checks."
